[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "stock-sdk"
version = "0.1.0"
description = "Personal stock data SDK (HK via Yahoo chart + Decodo proxy failover)"
requires-python = ">=3.9"
dependencies = ["requests", "pandas"]
